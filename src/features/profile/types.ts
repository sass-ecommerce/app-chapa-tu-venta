/**
 * ========================================
 * PROFILE FEATURE - TYPES
 * ========================================
 */

// Re-export from auth types (User is shared between auth and profile)
export type { User, UpdateProfilePayload } from '../auth/types';
